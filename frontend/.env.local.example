# frontend/.env.local (값은 .env.local 에만 — 커밋 금지)
# 백엔드 베이스 URL (backend :8000)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# 목 모드 스위치: 1 = 클러스터 의존 호출을 목/시드로 대체(백엔드 없이 6단계 시연), 0 = 실제 백엔드 연결
NEXT_PUBLIC_MOCK=0
