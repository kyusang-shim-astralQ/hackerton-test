# frontend/.env.local.example — 복사해서 .env.local 로 사용(.env.local 은 gitignore).
# 백엔드 베이스 URL (기본 http://localhost:8000)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# 1 = 클러스터 의존 호출을 목으로 대체(클러스터 없이 6단계 전체 시연).
# 0 = 실제 백엔드 연결(USE_SGE=1 인 backend).
NEXT_PUBLIC_MOCK=0
